@extends('layout')
@section('content')
  <h2>Microsoft 365 Mail — Laravel</h2>
  {{-- @if($authed)
    <p>Signed in. Go to <a href="{{ route('inbox') }}">Inbox</a> or <a href="{{ route('send.form') }}">Send Mail</a>.</p>
  @else
    <p><a href="{{ route('login') }}">Sign in with Microsoft</a></p>
  @endif --}}
@endsection
